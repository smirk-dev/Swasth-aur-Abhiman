name: swastha_aur_abhiman
description: A comprehensive digital ecosystem for health, education, and training services
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # Navigation
  go_router: ^13.0.0
  
  # Networking
  dio: ^5.4.0
  retrofit: ^4.0.3
  
  # Local Storage
  shared_preferences: ^2.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # UI Components
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  shimmer: ^3.0.0
  youtube_player_flutter: ^8.1.2
  flutter_animate: ^4.2.0
  smooth_page_indicator: ^1.1.0
  share_plus: ^7.2.1
  flutter_rating_bar: ^4.0.1

  # Video Player
  video_player: ^2.8.2
  chewie: ^1.7.4
  youtube_player_flutter: ^9.1.1
  
  # Audio Recording & Playback
  record: ^5.1.2
  audioplayers: ^6.1.0
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  
  # PDF Viewer
  flutter_pdfview: ^1.3.2
  
  # Charts
  fl_chart: ^0.66.0
  
  # WebSocket for Chat
  socket_io_client: ^2.0.3+1
  
  # Authentication
  jwt_decoder: ^2.0.1
  
  # Utilities
  intl: ^0.19.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  logger: ^2.0.2+1
  url_launcher: ^6.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  retrofit_generator: ^8.0.4
  json_serializable: ^6.7.1
  freezed: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
